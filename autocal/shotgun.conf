# Full-auto shotgun run specs. One run per line.
--sweep-metric outlier_ratio --sigma-source point
#--sweep-metric outlier_ratio --sigma-source origin
#--sweep-metric outlier_ratio --sigma-source min
--sweep-metric mad --sigma-source point
#--sweep-metric mad --sigma-source origin
#--sweep-metric mad --sigma-source min
--sweep-metric median_abs --sigma-source point
#--sweep-metric median_abs --sigma-source origin
#--sweep-metric median_abs --sigma-source min
